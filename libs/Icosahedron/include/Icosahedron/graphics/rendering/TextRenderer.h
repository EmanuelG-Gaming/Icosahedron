#ifndef IC_TEXT_RENDERER_H
#define IC_TEXT_RENDERER_H


namespace ic {
    namespace TextRenderer {
        //void initialize(int vertexCount);

        //void allocate_quads(int count);

        //void draw_refresh();

        ///** @brief Empties both the vertex and index buffers. */
        //void empty_buffers();

        

    };
}

#endif